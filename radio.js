// Generated by CoffeeScript 1.3.1
(function() {
  var add_player_box, fetch_feeds;

  fetch_feeds = function() {
    var feed, feeds, i, link, song, _i, _len;
    feeds = jQuery('.fbGroupsStream li.uiUnifiedStory');
    i = 1;
    for (_i = 0, _len = feeds.length; _i < _len; _i++) {
      feed = feeds[_i];
      link = jQuery(feed).find('.uiAttachmentTitle a');
      if (link.length > 0) {
        song = $(link[0]);
        console.log(i + ' ' + song.html() + ' ' + song.attr('href'));
        i++;
      }
    }
    return console.log(feeds.length);
  };

  add_player_box = function() {
    var player_wrap;
    player_wrap = $('<div style="width:100px;height:200px;position:absolute;top:0;left:50%;background-color:#ff0;">Hello!</div>');
    return $('body').append(player_wrap);
  };

  $(document).ready(function() {
    return fetch_feeds();
  });

}).call(this);
